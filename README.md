# stellar-horizon
Stellar Horizon is a lightweight, read-only utility for inspecting the Base Sepolia network. It provides developers with essential features to check network connectivity, inspect wallet balances, and validate testnet contract deployments in a safe, non-invasive manner
